---
layout: default
title: Interviews
---

<h2>Interviews</h2>
<p>There are currently <%= site.interviews.length %> interviews on the site.</p>

<h3>By date</h3>
<ul class="years">
<% site.archives.each do |year, data| %>
  <li>
    <a href="/interviews/<%= year %>/" title="View all interviews from <%= year %>."><strong><%= year %></strong></a>
    <ul class="months">
    <% data[:months].each_key do |month| %>
      <li><a href="/interviews/<%= year %>/<%= month %>/" title="View all interviews from x, <%= year %>."><%= month %></a></li>
    <% end %>
    </ul>
  </li>
<% end %>
</ul>

<h3>By category</h3>
<ul class="categories">
<% site.categories.keys.sort.each do |category| %>
  <li><a href="/interviews/<%= category %>" rel="category" title="View all <%= category.capitalize %> interviews."><%= category %></a><li>
<% end %>
</ul>