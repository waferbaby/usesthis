---
layout: default
title: Interviews
---

<h3>All the interviews</h3>
<p>There are currently <strong><%= @site.posts.length %></strong> interviews in <strong><%= @site.categories.length %></strong> categories, mentioning <strong><%= @site.hardware.length %></strong> bits of hardware and <strong><%= @site.software.length %></strong> bits of software.</p>

<% @site.archives.each do |year_key, year| %>
  <% year[:months].each do |month_key, month| %>
  <h4><%= month[:name] %> <%= year_key %></h4>
  <ul class="interviews">
    <% month[:posts].each do |interview| %>
    <li>
      <a href="http://<%= interview.slug %>.usesthis.com/" title="View the interview with <%= interview.name %>."><%= interview.name %></a><br>
      <span class="summary"><%= interview.summary %></span>
      <ul class="categories">
      <% interview.categories.each do |category| %><li><a href="/interviews/<%= category %>/" title="View all the <%= category.capitalize %> interviews."><%= category %></a></li><% end %>
      </ul>
    </li>
    <% end %>
  </ul>
  <% end %>
<% end %>