#!/usr/bin/env ruby

source_path = File.dirname(__dir__)
$:.unshift(source_path)

require 'lib/usesthis'

site = Salt::Site.instance

site.setup('./', {
  classes: [UsesThis::Interview]
})

site.generate