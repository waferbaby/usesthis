{
  "interviews": [
    <% @items.each do |interview| %>
    {
      <%= render_template('api.shared.interview', interview: interview) %>
    }<%= ',' unless interview == @items.last %>
    <% end %>
  ],
  "paging": {
    <%= render_template('api.shared.paging', pagination: @pagination) %>
  }
}