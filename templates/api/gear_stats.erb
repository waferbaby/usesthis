{
  "gear": [
    <% @stats.each do |stat| %>
    {
      <%= render_template('api.shared.ware', ware: stat[:ware]) %>,
      "count": <%= stat[:count] %>
    }<%= ',' unless stat == @stats.last %>
    <% end %>
  ]
}