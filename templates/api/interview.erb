{
  "interview": {
    <%= render_template('api.shared.interview', interview: @interview) %>,
    "contents": <%= @interview.contents.to_json %>,
    "gear": {
      "hardware": [
        <% @interview.hardware.each_value do |ware| %>
        {
          <%= render_template('api.shared.ware', ware: ware) %>
        }<%= ',' unless ware.slug == @interview.hardware.keys.last.to_s %>
        <% end %>
      ],
      "software": [
        <% @interview.software.each_value do |ware| %>
        {
          <%= render_template('api.shared.ware', ware: ware) %>
        }<%= ',' unless ware.slug == @interview.software.keys.last.to_s %>
        <% end %>
      ]
    }
  }
}